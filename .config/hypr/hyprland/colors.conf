# exec = export SLURP_ARGS='-d -c F5DDDABB -b 53434144 -s 00000000'

general {
    col.active_border = rgba(E9E1DF39)
    col.inactive_border = rgba(968F8E30)
}

misc {
    background_color = rgba(161312FF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(161312FF)
        col.text = rgba(E9E1DFFF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(E9E1DF), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(E9E1DF), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(E9E1DF), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(DFBFBAAA) rgba(DFBFBA77),pinned:1